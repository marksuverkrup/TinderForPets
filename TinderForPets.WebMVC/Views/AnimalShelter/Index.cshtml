@model IEnumerable<TinderForPets.Models.AnimalShelter.AnimalShelterListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Animal Shelters</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="paper">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShelterId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimeAdded)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ShelterId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TimeAdded)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ShelterId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ShelterId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ShelterId })
                </td>
            </tr>
        }

    </table>
</div>